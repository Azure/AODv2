# Runtime dependencies of the daemon (src/).
numpy>=1.21.0
PyYAML>=6.0
zstandard>=0.19.0

# Auto-upload to customer-owned Azure storage.
# Only needed when upload.enabled is true; the daemon degrades to
# "upload disabled" if these are absent. See docs/UPLOAD_DESIGN.md (D7).
azure-storage-blob>=12.19.0
azure-identity>=1.15.0

# Used only by the analysis scripts under tests/, not by the daemon.
pandas>=1.5.0
matplotlib>=3.5.0
